# Path to the file (change this to your desired location)
$filePath = "C:\Users\juraj_iptfkru\Downloads\executed.exe"

# Print message to console
Write-Output "executed"

# Write message to the file (creates the file if it doesn't exist)
"executed" | Out-File -FilePath $filePath -Encoding UTF8 -Append
